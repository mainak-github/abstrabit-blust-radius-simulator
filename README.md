# abstrabit-blust-radius-simulator
A distributed system simulator to model microservice dependencies, visualize service graphs, simulate failures, analyze blast radius, detect circular dependencies, calculate impact severity, and explore dependency paths with real-time updates and historical simulations.
